#include<stdio.h>
#include<string.h>
#define MAX_LN 2000001
int main()
{
	char S[MAX_LN];
	unsigned int q,i,ln;
	gets(S);
	scanf("%u",&q);
	ln=strlen(S);
	while(q>=0)
	{
		scanf("%u",&i);
		
		q--;
	}
	return 0;
}